@model IEnumerable<ACMWeb.Models.LO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Department.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LearningOutcome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LODesc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Department.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LearningOutcome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LODesc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LOID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LOID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LOID })
        </td>
    </tr>
}

</table>
